{
  "config": {
    "step": {
      "user": {
        "title": "Webhook 通知",
        "description": "配置 Webhook URL，通过 HTTP POST 发送通知消息。",
        "data": {
          "webhook_url": "Webhook URL",
          "name": "服务名称（仅英文/数字/下划线，选预设时自动生成）",
          "auth_token": "认证令牌（可选）",
          "headers": "自定义请求头（可选，JSON 格式）",
          "template_preset": "消息模板预设",
          "payload_template": "自定义模板（JSON + Jinja2，仅\"自定义\"预设时填写）"
        }
      }
    },
    "error": {
      "invalid_url": "URL 必须以 http:// 或 https:// 开头",
      "invalid_json": "请求头必须是有效的 JSON 格式",
      "invalid_template": "模板必须能渲染为有效的 JSON 格式",
      "invalid_name": "仅支持英文、数字和下划线（HA 会将中文转拼音导致无法辨认）"
    },
    "abort": {}
  }
}
