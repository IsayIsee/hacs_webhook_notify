{
  "config": {
    "step": {
      "user": {
        "title": "Webhook Notify",
        "description": "Configure a webhook URL to receive notifications via HTTP POST.",
        "data": {
          "webhook_url": "Webhook URL",
          "name": "Service Name (optional)",
          "auth_token": "Auth Token (optional)",
          "headers": "Custom Headers (optional, JSON)",
          "template_preset": "Message Template Preset",
          "payload_template": "Custom Template (JSON + Jinja2, only for 'Custom' preset)"
        }
      }
    },
    "error": {
      "invalid_url": "URL must start with http:// or https://",
      "invalid_json": "Headers must be valid JSON",
      "invalid_template": "Template must render to valid JSON"
    },
    "abort": {}
  }
}
